<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
			 xmlns:i="http://www.springframework.org/schema/integration"
			 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
			 xsi:schemaLocation="
           http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
       ">
	<i:channel id="recipes"/>

	<!-- replace with splitter here -->
	<i:bridge input-channel="recipes" output-channel="ingredients"/>

	<i:channel id="ingredients"/>

	<!-- replace with shopping-flow.xml -->
	<i:bridge input-channel="ingredients" output-channel="products"/>

	<i:channel id="products"/>

	<!-- replace with cooking flow -->
	<i:bridge input-channel="products" output-channel="meals"/>

	<i:channel id="meals">
		<i:queue capacity="50"/>
	</i:channel>

</beans>